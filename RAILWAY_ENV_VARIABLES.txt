# Railway Environment Variables - النسخة الصحيحة
# انسخ هذه القيم إلى Railway Dashboard → Variables

# ============================================
# إعدادات التطبيق الأساسية
# ============================================
APP_NAME=Complaints System
APP_ENV=production
APP_KEY=base64:ljpCopA2CivTtb89ogzByXMvBe+9ip9DhINxWXQfNs4=
APP_DEBUG=false
APP_URL=https://your-app-name.railway.app

# ============================================
# إعدادات قاعدة البيانات (من Railway Database)
# ============================================
DB_CONNECTION=mysql
DB_HOST=nozomi.proxy.rlwy.net
DB_PORT=31686
DB_DATABASE=railway
DB_USERNAME=root
DB_PASSWORD=IUyaoVFSuViYPKoxOsIggfuGqlyEgDYD

# ============================================
# إعدادات البريد الإلكتروني (Gmail SMTP)
# ============================================
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=khader.diwani@gmail.com
MAIL_PASSWORD=pvlplhnymbfxwunr
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=khader.diwani@gmail.com
MAIL_FROM_NAME=Complaints System

# ============================================
# إعدادات التخزين (S3 - الأفضل للإنتاج)
# ============================================
# إذا لم يكن لديك S3، اترك هذه القيم فارغة واستخدم local
FILESYSTEM_DISK=local
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_URL=

# ============================================
# إعدادات Queue
# ============================================
QUEUE_CONNECTION=database

# ============================================
# إعدادات Cache (استخدم file على Railway إذا لم يكن لديك Redis)
# ============================================
CACHE_DRIVER=file
SESSION_DRIVER=file
SESSION_LIFETIME=120

# ============================================
# إعدادات Logging
# ============================================
LOG_CHANNEL=stack
LOG_LEVEL=error
LOG_DEPRECATIONS_CHANNEL=null

# ============================================
# إعدادات أخرى
# ============================================
BROADCAST_DRIVER=log
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379
REDIS_CLIENT=predis

# ============================================
# Firebase (إذا كنت تستخدمه)
# ============================================
FIREBASE_CREDENTIALS=storage/app/firebase/firebase_credentials.json
FIREBASE_PROJECT_ID=complaint-naya

# ============================================
# Build Command (في Railway Settings)
# ============================================
# NIXPACKS_BUILD_CMD=composer install --no-dev --optimize-autoloader && npm install --production && php artisan optimize && php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan storage:link

# ============================================
# ملاحظات مهمة:
# ============================================
# 1. استبدل APP_URL بـ URL المشروع الفعلي من Railway
# 2. إذا أردت استخدام S3، املأ AWS_* variables
# 3. MAIL_PASSWORD يجب أن يكون App Password من Gmail (ليس كلمة المرور العادية)
# 4. تأكد من تشغيل Queue Worker على Railway

