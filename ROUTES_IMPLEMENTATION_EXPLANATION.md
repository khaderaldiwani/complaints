# شرح تنفيذ Routes في النظام

## نظرة عامة

تم تنظيم Routes في النظام إلى ثلاث فئات رئيسية:
1. **Routes عامة**: لا تتطلب مصادقة (تسجيل دخول، تسجيل حساب)
2. **Routes محمية**: تتطلب مصادقة باستخدام Laravel Sanctum
3. **Route خاص**: للاختبار فقط

---

## القسم الأول: Routes عامة (بدون مصادقة)

### 1. تسجيل حساب مواطن (`POST /api/register`)

**الهدف**: إنشاء حساب جديد للمواطنين

**آلية التنفيذ**:
- يستقبل بيانات التسجيل (الاسم، البريد الإلكتروني، كلمة المرور)
- يتحقق من عدم وجود حساب بنفس البريد مسبقاً
- ينشئ حساب جديد مع تشفير كلمة المرور
- يولد كود OTP عشوائي مكون من 6 أرقام
- يخزن الكود مع تاريخ انتهاء (10 دقائق)
- يرسل الكود عبر البريد الإلكتروني
- يُرجع بيانات المستخدم بدون كلمة المرور

**ملاحظة**: الحساب يُنشأ غير موثق ويحتاج إلى التحقق عبر OTP

---

### 2. التحقق من OTP (`POST /api/verify-otp`)

**الهدف**: تفعيل حساب المواطن بعد التحقق من الكود

**آلية التنفيذ**:
- يستقبل معرف المستخدم وكود التحقق
- يتحقق من وجود المستخدم وعدم تفعيله مسبقاً
- يتحقق من صحة الكود وأنه لم ينتهِ صلاحيته
- في حال النجاح: يفعل الحساب ويُزيل كود التحقق
- يُرجع رسالة نجاح مع Token للمستخدم (للأستخدام الفوري)

**الحماية**: الكود صالح لمدة 10 دقائق فقط

---

### 3. إعادة إرسال OTP (`POST /api/resend-otp`)

**الهدف**: إعادة إرسال كود التحقق في حالة عدم وصوله

**آلية التنفيذ**:
- يستقبل معرف المستخدم
- يتحقق من وجود المستخدم
- يولد كود جديد ويحدد تاريخ انتهاء جديد
- يرسل الكود الجديد عبر البريد
- يُرجع رسالة تأكيد

**الحد**: يمكن استخدامه عدة مرات عند الحاجة

---

### 4. تسجيل دخول مواطن (`POST /api/login`)

**الهدف**: تسجيل دخول المواطنين المسجلين

**آلية التنفيذ**:
- يستقبل البريد الإلكتروني وكلمة المرور
- يبحث عن المستخدم ويتحقق من كلمة المرور
- يتحقق من أن الحساب مفعل
- في حال النجاح: ينشئ Token ويُرجعه مع بيانات المستخدم
- في حال الفشل: يُرجع رسالة خطأ عامة

**الأمان**: كلمة المرور مشفرة ومقارنتها آمنة

---

### 5. تسجيل دخول إداري (`POST /api/admin/login`)

**الهدف**: تسجيل دخول المديرين والموظفين

**آلية التنفيذ**:
- يستقبل اسم المستخدم وكلمة المرور
- يبحث في جدول المستخدمين الإداريين
- يتحقق من صحة كلمة المرور
- في حال النجاح: ينشئ Token للمستخدم الإداري ويُرجعه
- يُرجع بيانات المستخدم الإداري (الاسم، الدور، الجهة)

**الفرق عن تسجيل دخول المواطن**: يستخدم اسم المستخدم بدلاً من البريد، ولا يتطلب OTP

---

## القسم الثاني: Routes محمية (بمصادقة Sanctum)

جميع Routes التالية تتطلب إرسال Token في Header: `Authorization: Bearer {token}`

### أ. إدارة الجلسات

### 6. تسجيل الخروج مواطن (`POST /api/logout`)

**الهدف**: إنهاء جلسة المستخدم

**آلية التنفيذ**:
- يحذف Token الحالي من قاعدة البيانات
- يُرجع رسالة نجاح

---

### 7. تسجيل الخروج إداري (`POST /api/admin/logout`)

**الهدف**: إنهاء جلسة المستخدم الإداري

**نفس آلية تسجيل خروج المواطن**

---

### 8. معلومات المستخدم (`GET /api/user`)

**الهدف**: الحصول على بيانات المستخدم المسجل دخوله

**آلية التنفيذ**:
- يستخدم Token الموجود في Header
- يُرجع بيانات المستخدم مباشرة من `$request->user()`

**مثال الاستخدام**: للتحقق من صحة Token أو جلب معلومات المستخدم

---

### ب. إدارة الشكاوى - للمواطنين

### 9. إضافة شكوى (`POST /api/complaints/store`)

**الهدف**: إنشاء شكوى جديدة من قبل المواطن

**آلية التنفيذ**:
- يستقبل بيانات الشكوى (النوع، العنوان، الوصف، الجهة)
- يستقبل ملف اختياري (صورة أو مستند)
- يحفظ الملف في التخزين إذا وُجد
- ينشئ الشكوى في قاعدة البيانات مع ربطها بالمستخدم المسجل دخوله
- يحدد الحالة الافتراضية كـ "جديدة"
- يُفرغ الكاش المتعلق بالإحصائيات وقوائم الشكاوى
- يُرجع بيانات الشكوى المُنشأة

**الحماية**: يمكن للمستخدم فقط ربط الشكوى بحسابه

---

### 10. قائمة الشكاوى حسب الحالة (`GET /api/complaints/status/{status}`)

**الهدف**: جلب شكاوى المستخدم حسب حالتها

**آلية التنفيذ**:
- يستخدم الكاش للحصول على البيانات (5 دقائق صلاحية)
- يفلتر الشكاوى حسب معرف المستخدم المسجل دخوله والحالة المطلوبة
- يُرجع قائمة الشكاوى

**الحالات المتاحة**: 1 (جديدة)، 2 (قيد المعالجة)، 3 (مكتملة)، 4 (مرفوضة)

---

### 11. تفاصيل شكوى (`GET /api/complaints/details/{id}`)

**الهدف**: الحصول على تفاصيل شكوى محددة

**آلية التنفيذ**:
- يستخدم الكاش للحصول على البيانات
- يتحقق من أن الشكوى تخص المستخدم المسجل دخوله
- في حال عدم المطابقة: يُرجع خطأ 404
- يُرجع تفاصيل الشكوى الكاملة

**الأمان**: المستخدم لا يمكنه رؤية شكاوى الآخرين

---

### ج. إدارة الشكاوى - للموظفين

### 12. قائمة الجهات (`GET /api/agencies`)

**الهدف**: جلب جميع الجهات المتاحة

**آلية التنفيذ**:
- يستخدم الكاش للحصول على البيانات (ساعة صلاحية)
- يُرجع قائمة بجميع الجهات

**الاستخدام**: لاختيار الجهة عند إضافة شكوى

---

### 13. شكاوى جهة الموظف (`GET /api/employee/complaints`)

**الهدف**: جلب جميع شكاوى الجهة الخاصة بالموظف

**آلية التنفيذ**:
- يتحقق من أن المستخدم موظف (role = 2)
- يتحقق من ربط الموظف بجهة
- يستخدم الكاش للحصول على البيانات
- يفلتر الشكاوى حسب جهة الموظف
- يُرجع قائمة الشكاوى

**القيد**: الموظف يرى فقط شكاوى جهته

---

### 14. شكاوى الجهة حسب الحالة (`GET /api/employee/complaints/status/{status}`)

**الهدف**: جلب شكاوى الجهة مصنفة حسب الحالة

**نفس آلية الطلب السابق مع إضافة فلترة حسب الحالة**

---

### 15. تفاصيل شكوى للموظف (`GET /api/employee/complaints/{id}`)

**الهدف**: عرض تفاصيل شكوى للمعالجة

**آلية التنفيذ**:
- يتحقق من أن المستخدم موظف تابع للجهة
- يتحقق من أن الشكوى تتبع الجهة
- يتحقق من عدم حجزها من موظف آخر (أو انتهاء وقت الحجز - 10 دقائق)
- عند فتح الشكوى: يقوم بحجزها للموظف الحالي
- يُرجع تفاصيل الشكوى

**نظام الحجز**: يمنع تعديل موظفين مختلفين للشكوى نفسها في نفس الوقت

---

### 16. تحديث حالة الشكوى (`PUT /api/employee/complaints/{id}/status`)

**الهدف**: تغيير حالة الشكوى (قيد المعالجة، مكتملة، مرفوضة)

**آلية التنفيذ**:
- يتحقق من صلاحيات الموظف والجهة
- يتحقق من عدم حجز الشكوى من موظف آخر
- يسجل التغيير في سجل العمليات (Audit Log)
- يحدث الحالة في قاعدة البيانات
- يسجل التغيير في جدول سجل الشكوى (History)
- يرسل إشعارات للمواطن (في قاعدة البيانات + FCM) عبر نظام Queue
- يُفرغ الكاش المتعلق بالشكوى والإحصائيات
- يُرجع الشكوى المحدثة

**الحالات المسموحة**: 2 (قيد المعالجة)، 3 (مكتملة)، 4 (مرفوضة)

---

### 17. إضافة ملاحظة على الشكوى (`PUT /api/employee/complaints/{id}/note`)

**الهدف**: إضافة ملاحظة أو رد من الموظف على الشكوى

**آلية التنفيذ**:
- يتحقق من صلاحيات الموظف والجهة
- يتحقق من عدم حجز الشكوى من موظف آخر
- يسجل التغيير في سجل العمليات
- يحدث الملاحظة في قاعدة البيانات
- يسجل التغيير في سجل الشكوى
- يرسل إشعارات للمواطن عبر Queue
- يُفرغ الكاش
- يُرجع الشكوى المحدثة

---

### 18. حجز الشكوى (`POST /api/employee/complaints/{id}/lock`)

**الهدف**: حجز الشكوى للمعالجة (منع موظفين آخرين من تعديلها)

**آلية التنفيذ**:
- يتحقق من صلاحيات الموظف والجهة
- يتحقق من عدم حجزها من موظف آخر (أو انتهاء وقت الحجز)
- يسجل معرف الموظف الحالي ووقت الحجز
- يُرجع رسالة نجاح

**مدة الحجز**: 10 دقائق (بعدها يمكن لموظف آخر حجزها)

---

### 19. فك حجز الشكوى (`POST /api/employee/complaints/{id}/unlock`)

**الهدف**: إلغاء حجز الشكوى

**آلية التنفيذ**:
- يتحقق من أن الموظف هو من حجز الشكوى (أو أن المستخدم مدير)
- يُزيل الحجز (يضع null في الحقول)
- يُرجع رسالة نجاح

**الصلاحيات**: يمكن للموظف فك حجزه أو للمدير فك أي حجز

---

### 20. سجل الشكوى (`GET /api/employee/complaints/{id}/history`)

**الهدف**: عرض تاريخ تغييرات الشكوى

**آلية التنفيذ**:
- يتحقق من صلاحيات الموظف أو المدير
- يتحقق من أن الشكوى تتبع الجهة (للموظف)
- يجلب جميع السجلات من جدول سجل الشكوى
- يرتبها حسب التاريخ (الأحدث أولاً)
- يُرجع قائمة السجلات

**المحتوى**: كل تغيير في الحالة أو الملاحظات مع الموظف المسؤول والوقت

---

### د. إدارة المستخدمين - للمديرين

### 21. إنشاء حساب إداري (`POST /api/admin/register`)

**الهدف**: إنشاء حساب جديد للمديرين أو الموظفين

**آلية التنفيذ**:
- يتحقق من أن المستخدم الحالي مدير (role = 1)
- يستقبل بيانات الحساب الجديد
- يتحقق من عدم وجود اسم مستخدم مكرر
- يشفر كلمة المرور
- ينشئ الحساب في قاعدة البيانات
- يسجل العملية في سجل العمليات
- يُفرغ كاش الحسابات
- يُرجع بيانات الحساب المُنشأ

**الصلاحيات**: المدير فقط يمكنه إنشاء حسابات إدارية

---

### 22. قائمة الحسابات الإدارية (`GET /api/admin/accounts`)

**الهدف**: عرض جميع الحسابات الإدارية

**آلية التنفيذ**:
- يتحقق من أن المستخدم مدير
- يستخدم الكاش للحصول على البيانات
- يجلب جميع الحسابات مع معلومات الجهة المرتبطة
- يُرجع القائمة

---

### 23. تحديث حساب إداري (`PUT /api/admin/accounts/{id}`)

**الهدف**: تعديل بيانات حساب إداري

**آلية التنفيذ**:
- يتحقق من صلاحيات المدير
- يتحقق من وجود الحساب
- يحدث البيانات المطلوبة
- إذا تم تغيير كلمة المرور: يشفرها
- يسجل التغييرات في سجل العمليات
- يُفرغ الكاش
- يُرجع الحساب المحدث

---

### 24. حذف حساب إداري (`DELETE /api/admin/accounts/{id}`)

**الهدف**: حذف حساب إداري

**آلية التنفيذ**:
- يتحقق من صلاحيات المدير
- يتحقق من وجود الحساب
- يسجل العملية قبل الحذف
- يحذف الحساب من قاعدة البيانات
- يُفرغ الكاش
- يُرجع رسالة نجاح

---

### 25. تغيير حالة حساب إداري (`POST /api/admin/accounts/{id}/status`)

**الهدف**: تفعيل أو إيقاف حساب إداري

**آلية التنفيذ**:
- يتحقق من صلاحيات المدير
- يستقبل الحالة الجديدة (مفعل/معطل)
- يحدث الحالة
- يسجل التغيير في سجل العمليات
- يُفرغ الكاش
- يُرجع الحساب المحدث

**الاستخدام**: لإيقاف موظف مؤقتاً بدلاً من حذف حسابه

---

### هـ. إدارة المواطنين - للمديرين

### 26. قائمة المواطنين (`GET /api/admin/users`)

**الهدف**: عرض جميع المواطنين المسجلين

**آلية التنفيذ**:
- يتحقق من أن المستخدم مدير
- يستخدم الكاش للحصول على البيانات
- يجلب جميع المستخدمين مع معلوماتهم
- يُرجع القائمة

---

### 27. تعطيل حساب مواطن (`POST /api/admin/users/{id}/disable`)

**الهدف**: تعطيل حساب مواطن

**آلية التنفيذ**:
- يتحقق من صلاحيات المدير
- يحدث حالة الحساب إلى معطل
- يسجل العملية
- يُرجع رسالة نجاح

**التأثير**: المواطن لا يستطيع تسجيل الدخول

---

### 28. تفعيل حساب مواطن (`POST /api/admin/users/{id}/enable`)

**الهدف**: إعادة تفعيل حساب مواطن معطل

**نفس آلية التعطيل مع تغيير الحالة إلى مفعل**

---

### 29. حذف حساب مواطن (`DELETE /api/admin/users/{id}`)

**الهدف**: حذف حساب مواطن نهائياً

**آلية التنفيذ**:
- يتحقق من صلاحيات المدير
- يسجل العملية
- يحذف الحساب من قاعدة البيانات
- يُفرغ الكاش
- يُرجع رسالة نجاح

---

### و. عرض الشكاوى - للمديرين

### 30. جميع الشكاوى (`GET /api/admin/complaints`)

**الهدف**: عرض جميع الشكاوى في النظام

**آلية التنفيذ**:
- يتحقق من أن المستخدم مدير
- يجلب جميع الشكاوى مع معلومات المستخدم والجهة
- يرتبها حسب التاريخ (الأحدث أولاً)
- يُرجع القائمة الكاملة

**الفرق**: المدير يرى كل الشكاوى، الموظف يرى فقط شكاوى جهته

---

### ز. الإشعارات - للمستخدمين

### 31. قائمة الإشعارات (`GET /api/user/notifications`)

**الهدف**: جلب جميع إشعارات المستخدم

**آلية التنفيذ**:
- يستخدم الكاش للحصول على البيانات
- يجلب جميع الإشعارات للمستخدم المسجل دخوله
- يرتبها حسب التاريخ (الأحدث أولاً)
- يُرجع القائمة

---

### 32. تحديد إشعار كمقروء (`POST /api/user/notifications/read/{id}`)

**الهدف**: تحديد أن المستخدم قرأ الإشعار

**آلية التنفيذ**:
- يتحقق من أن الإشعار يخص المستخدم
- يحدث حالة الإشعار إلى "مقروء"
- يُفرغ الكاش
- يُرجع رسالة نجاح

---

### ح. التقارير والإحصائيات - للمديرين

### 33. الإحصائيات (`GET /api/admin/statistics`)

**الهدف**: عرض إحصائيات شاملة عن النظام

**آلية التنفيذ**:
- يتحقق من أن المستخدم مدير
- يستخدم الكاش للحصول على البيانات (10 دقائق صلاحية)
- يحسب:
  - إجمالي الشكاوى
  - الشكاوى حسب الحالة
  - الشكاوى حسب الجهة
  - عدد المواطنين والمديرين والموظفين
  - الشكاوى اليومية والشهرية
- يُرجع جميع الإحصائيات

**الأداء**: استخدام الكاش يقلل الضغط على قاعدة البيانات

---

### 34. تقرير الشكاوى PDF (`GET /api/admin/reports/complaints`)

**الهدف**: توليد تقرير PDF بجميع الشكاوى

**آلية التنفيذ**:
- يتحقق من أن المستخدم مدير
- يجلب جميع الشكاوى مع معلومات المستخدم والجهة
- يستخدم مكتبة DomPDF لإنشاء ملف PDF
- يُستخدم قالب Blade لعرض البيانات
- يُرجع ملف PDF للتحميل

**الاستخدام**: للطباعة أو الأرشيف

---

### 35. سجل العمليات (`GET /api/admin/audit-logs`)

**الهدف**: عرض سجل جميع العمليات الإدارية

**آلية التنفيذ**:
- يتحقق من أن المستخدم مدير
- يجلب جميع السجلات من جدول سجل العمليات
- يرتبها حسب التاريخ
- يُرجع القائمة

**المحتوى**: كل عملية (إنشاء، تحديث، حذف) مع المستخدم المسؤول والقيم القديمة والجديدة

---

## القسم الثالث: Routes خاصة

### 36. اختبار FCM (`POST /api/test-fcm`)

**الهدف**: اختبار إرسال الإشعارات عبر Firebase Cloud Messaging

**آلية التنفيذ**:
- يستقبل الموضوع والعنوان والمحتوى
- يرسل الإشعار عبر FCM
- يُرجع استجابة FCM

**ملاحظة**: هذا Route للاختبار فقط ويجب إزالته في الإنتاج

---

## ملخص آلية الحماية

### 1. المصادقة (Authentication)
- جميع Routes المحمية تتطلب Token في Header
- Laravel Sanctum يتحقق تلقائياً من صحة Token
- في حال عدم وجود Token أو عدم صحته: يُرجع خطأ 401

### 2. التفويض (Authorization)
- كل Controller يتحقق من صلاحيات المستخدم
- الموظفون يقتصرون على شكاوى جهتهم فقط
- المديرون لديهم صلاحيات كاملة

### 3. التحقق من البيانات (Validation)
- جميع Routes تتحقق من صحة البيانات المدخلة
- في حال الخطأ: يُرجع خطأ 422 مع تفاصيل

### 4. الأمان
- كلمات المرور مشفرة دائماً
- Tokens مشفرة في قاعدة البيانات
- رسائل خطأ عامة (لا تكشف معلومات حساسة)

---

## أنماط الاستجابة

جميع Routes تتبع نمط موحد للاستجابة:

**نجاح**:
```json
{
    "success": true,
    "message": "رسالة النجاح",
    "data": {...},
    "status_code": 200,
    "timestamp": "2025-12-10T14:30:00+00:00"
}
```

**فشل**:
```json
{
    "success": false,
    "message": "رسالة الخطأ",
    "data": null,
    "status_code": 401,
    "timestamp": "2025-12-10T14:30:00+00:00"
}
```

---

## الخلاصة

النظام يستخدم بنية RESTful API منظمة مع:
- فصل واضح بين Routes العامة والمحمية
- نظام صلاحيات متعدد المستويات
- استخدام الكاش لتحسين الأداء
- سجل شامل للعمليات
- نظام إشعارات متكامل
- استجابات موحدة ومنظمة

